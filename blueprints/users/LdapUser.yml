---
title: LdapUser
description: Account is externally managed via LDAP server

sections:
  ldap_mail:
    type: info
    label: Mail
    text: "{{ user.email }}"
  ldap_name:
    type: info
    label: Name
    text: "{{ user.name }}"
