---
title: LdapUser
description: Account is externally managed via LDAP server

sections:
  #ldap_dn:
  #  type: info
  #  label: DN
  #  text: "{{ user.dn }}"
  #ldap_uid:
  #  type: info
  #  label: UID
  #  text: "{{ user.uid }}"
  ldap_mail:
    type: info
    label: Mail
    text: "{{ user.email }}"
  ldap_name:
    type: info
    label: Name
    text: "{{ user.name }}"
