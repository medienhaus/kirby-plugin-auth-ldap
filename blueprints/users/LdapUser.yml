title: LdapUser
description: Account is externally managed via LDAP server

sections:
  ldap_dn:
    type: info
    label: DN
    text: "{{ user.getLdapDn }}"
  #ldap_mail:
  #  type: info
  #  label: Mail
  #  text: "{{ user.getLdapDn }}"
  #ldap_name:
  #  type: info
  #  label: UID
  #  text: "{{ user.getLdapDn }}"
